node {
    stage('Clone sources') {
        git url: 'https://github.com/aretae/javaSample.git'

    stage('Gradle build') {
        buildInfo = rtGradle.run rootDir: "gradle-examples/4/gradle-example-ci-server/", buildFile: 'build.gradle', tasks: 'clean build'
    }

}